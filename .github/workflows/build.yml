name: build
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v2
     - run: apt-get update
     - run: apt install make gcc
     - run: make menuconfig
     - run: make
name: build
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v2
     - run: sudo apt-get update
     - run: sudo apt install make gcc
     - run: make menuconfig
     - run: make